org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# --- Find My Bay ---
# Override these in ~/.gradle/gradle.properties or local.properties (not committed)
# Get a key from https://console.cloud.google.com/google/maps-apis
MAPS_API_KEY=AIzaSyCikUz4b5IUZ3RDsjAXWsvHwsHsN01xFvM

# Pointing at your local backend during dev. Override per-environment.
# 10.0.2.2 is the Android emulator's loopback to the host machine — use this
# when running the backend on the same laptop as Android Studio. For a real
# device on your wifi, swap this for your laptop's LAN IP, e.g. http://192.168.1.42:3000/
API_BASE_URL=http://10.0.2.2:3000/
