org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# --- Find My Bay ---
# Override these in ~/.gradle/gradle.properties or local.properties (not committed)
# Get a key from https://console.cloud.google.com/google/maps-apis
MAPS_API_KEY=AIzaSyCikUz4b5IUZ3RDsjAXWsvHwsHsN01xFvM

# Production Azure Container App. Same backend the vendor admin SPA hits.
# For local dev override via ~/.gradle/gradle.properties or local.properties:
#   API_BASE_URL=http://10.0.2.2:3000/      # emulator → laptop's npm run dev
#   API_BASE_URL=http://192.168.x.x:3000/   # real device → laptop's LAN IP
API_BASE_URL=https://fmb-backend.bravegrass-00d218a4.uaenorth.azurecontainerapps.io/
