# Attendant app — default R8 rules are sufficient for a Compose-only app.
# Add keep rules here if/when reflection-based libs (Retrofit, kotlinx
# serialization) are introduced in the networking phase.
