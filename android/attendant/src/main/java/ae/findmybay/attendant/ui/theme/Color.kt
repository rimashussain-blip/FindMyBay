package ae.findmybay.attendant.ui.theme

import androidx.compose.ui.graphics.Color

// findMy Bay brand palette — mirrors the attendant-app-shared.jsx `fmb` tokens
// and the customer app's Color.kt. Single source of truth for the attendant app.
val FmbPrimary     = Color(0xFF14B8A6) // aqua
val FmbPrimaryDeep = Color(0xFF0F766E) // deep teal
val FmbInk         = Color(0xFF0B3B36) // deep forest, primary text
val FmbInkSoft     = Color(0xFF5C7A75) // subtitle / meta
val FmbCream       = Color(0xFFFFF7EC) // warm light background
val FmbMint        = Color(0xFFE6F7F4) // soft surface tint
val FmbMintEdge    = Color(0xFFCDEEE8) // hairline borders
val FmbSand        = Color(0xFFFCE7C8) // warm chip / amber accents
val FmbSandDeep    = Color(0xFFF5C77E) // amber accent
val FmbCoral       = Color(0xFFFF8B6B) // alerts / urgent
val FmbCoralSoft   = Color(0xFFFFE3D9) // alert backgrounds
val FmbWhite       = Color(0xFFFFFFFF)

// Warm browns used for sand-chip text + the busy countdown.
val FmbSandText    = Color(0xFF7A4D12)

// Closed-bay greys (neutral, off-brand on purpose to read as "inactive").
val FmbClosedBg    = Color(0xFFF0EFEA)
val FmbClosedEdge  = Color(0xFFD5D3CC)
val FmbClosedInk   = Color(0xFF9C9A92)
