<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext HTTP traffic to specific dev hosts.
  10.0.2.2 = the host machine's localhost as seen from the Android emulator.
  localhost = on-device loopback (useful when running on a real device with adb reverse).

  Production traffic to api.findmybay.ae remains HTTPS-only thanks to the
  default <base-config cleartextTrafficPermitted="false">.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
